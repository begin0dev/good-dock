import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  formWrapper: {
    paddingBottom: 18,
  },
  formLabel: {
    paddingBottom: 6,
  },
  formController: {
    width: "100%",
    flexDirection: "row",
    alignItems: "center",
  },
});
